<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ir.mahozad.workout_logger">

  <application
      android:name=".WorkoutApplication"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.WorkoutLogger">
    <activity
        android:name=".ui.addUser.AddUserActivity"
        android:exported="false"/>
    <activity
        android:name=".ui.users.UsersActivity"
        android:exported="false"/>
    <activity
        android:name=".ui.userSelect.UserSelectionActivity"
        android:exported="false"/>
    <activity
        android:name=".ui.workout.WorkoutActivity"
        android:exported="false"/>
    <activity
        android:name=".ui.workouts.WorkoutsReportActivity"
        android:exported="false"/>
    <activity
        android:name=".ui.main.MainActivity"
        android:exported="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.WorkoutLogger.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>
